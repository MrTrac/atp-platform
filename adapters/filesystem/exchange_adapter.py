"""Filesystem exchange adapter skeleton for ATP v0."""


def write_exchange_bundle(bundle: dict) -> dict:
    """Return a minimal filesystem exchange result."""
    # TODO: write exchange bundle into runtime workspace.
    return {"bundle": bundle, "status": "seed"}
