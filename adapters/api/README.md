# API

`adapters/api` hiện là placeholder adapter cho ATP v0.

Phạm vi:

- giữ chỗ cho API-backed execution hoặc integration path trong architecture
- không materialize runtime behavior thật ở baseline hiện hành

Deferred có chủ đích:

- real API transport
- remote request/response orchestration
- API artifact exchange plane
