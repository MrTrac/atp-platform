# Manifest Templates

Reusable YAML templates for ATP context and task manifests.

| Template | Purpose |
|---|---|
| `product_context.template.yaml` | Product-specific context packaging |
| `task_manifest.template.yaml` | Task manifest with capabilities and evidence refs |

Used by `core/context/` to structure manifest artifacts.
