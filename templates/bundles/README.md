# Bundle Templates

Reusable YAML templates for ATP handoff bundles.

| Template | Purpose |
|---|---|
| `exchange_bundle.template.yaml` | Cross-phase exchange bundle for handoff |
| `evidence_bundle.template.yaml` | Evidence packaging for review/validation |

These templates define the structural shape that `core/handoff/` modules populate at runtime.
