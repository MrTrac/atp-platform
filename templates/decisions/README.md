# Decision Templates

Reusable YAML templates for ATP approval and review decisions.

| Template | Purpose |
|---|---|
| `approval_decision.template.yaml` | Human-gated approval decision record |
| `review_decision.template.yaml` | Operator review decision record |

Used by `core/approvals/` to structure decision artifacts.
