# Profiles

`profiles/` chứa product profiles cho ATP v0.

Phạm vi:

- profile metadata tối thiểu cho product resolution
- local context để nối product với repo boundary, policies, và runtime assumptions
