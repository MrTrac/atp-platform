# Registry

`registry/` chứa registry seeds cho ATP v0 gồm products, providers, capabilities, nodes, và policies.

Phạm vi:

- static lookup data phục vụ resolution và routing
- naming/shape ổn định cho control-plane baseline

Không thuộc scope:

- dynamic registry service
- runtime registry mutation
- discovery plane rộng
