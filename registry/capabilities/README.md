# Capabilities

`registry/capabilities` chứa capability registry seed cho ATP v0.

Dùng để:

- mô tả capability names phục vụ routing preparation
- giữ mapping ổn định giữa request intent và execution capability
