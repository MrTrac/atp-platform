policy_name: routing_policy
policy_type: routing
mode: capability_based
rules:
  - derive required capability from task manifest or request intent
  - prefer providers that support all required capabilities
  - prefer local_mac when a compatible local node exists
  - prefer non_llm_execution for local operational requests
