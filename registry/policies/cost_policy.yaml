policy_name: cost_policy
policy_type: cost_control
mode: local_first
rules:
  - prefer low cost local providers when capability support is equivalent
  - explain local-first selection in routing result
