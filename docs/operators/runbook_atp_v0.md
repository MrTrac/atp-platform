# Runbook ATP v0

Runbook M7 cho ATP:

1. tiep nhan request file
2. normalize request
3. classify request
4. resolve product
5. package context
6. prepare va select route
7. thuc thi local command neu route ho tro
8. capture raw va filtered execution artifacts
9. tao validation summary va review preview
10. xem summary tren CLI

Van hanh v0:

- artifact capture chi la dict-based, khong ghi vao `SOURCE_DEV/workspace`
- validation la rule-based summary nhe
- review chi co `accept` / `reject` / `revise`
- approval/finalization van chua duoc implement
