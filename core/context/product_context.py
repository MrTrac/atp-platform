"""Product context skeleton for ATP v0."""


def build_product_context(product: str) -> dict:
    """Return a minimal product context."""
    # TODO: package product identity and boundary context.
    return {"product": product, "context_type": "product_context"}
