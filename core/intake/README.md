# Intake

`core/intake` chứa request intake và normalization entry cho ATP v0.

Phạm vi:

- nhận request input tối thiểu
- normalize request shape phục vụ classification và các bước sau

Không thuộc scope:

- external intake gateway
- queueing / transport layer
