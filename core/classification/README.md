# Classification

`core/classification` chứa input classification tối thiểu cho ATP v0.

Phạm vi:

- phân loại request ở mức rule-based
- xác định execution intent và request type phục vụ resolution, routing, và validation flow

Không thuộc scope:

- classifier model phức tạp
- arbitration engine
- provider-specific intent inference
