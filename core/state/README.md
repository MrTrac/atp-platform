# State

`core/state` chứa run state và decision state summaries tối thiểu cho ATP v0.

Phạm vi:

- run record
- decision state summary
- close/continue-related state shaping phục vụ finalization và materialization

Không thuộc scope:

- persistence-backed state machine
- distributed run state coordination
